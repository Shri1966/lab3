#include <iostream>
using namespace std;

int main() {

int a, b, c, larger;
	cout<<"Enter three number : ";
	cin>>a>>b>>c;
	if(a>b && a>c)
	{larger=a;}
	if(b>a && b>c)
	{larger=b;}
	if(c>a && c>b)
	{larger=c;}
	cout<<"largest of the three number is "<<larger;
	return 0;
}
