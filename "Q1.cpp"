#include <iostream>
using namespace std;

int main() {

int a, b, larger;
	cout<<"Enter two number : ";
	cin>>a>>b;
	if(a>b)
	{larger=a;}
	if(b>a)
	{larger=b;}
	cout<<"largest of the two number is "<<larger;
	return 0;
}
